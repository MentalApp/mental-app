import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  padding: 10px 30px;
  .active {
    color: #29afaf;
  }
  .item-header {
    margin-right: 15px;
    cursor: pointer;
  }
`;

export default Wrapper;
